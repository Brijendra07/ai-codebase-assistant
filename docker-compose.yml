version: "3.9"

services:
  ai-codebase-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-codebase-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
